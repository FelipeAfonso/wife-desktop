#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

#autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=DP-3,3840x2160@120,0x0,1
# monitor=DP-3,3840x2160@120,0x0,2
# monitor=DP-3,1920x1080@120,0x0,1
# monitor=DP-1,3840x2160@60,0x0,1
# monitor=DP-1,3840x2160@110,0x0,1
# monitor=DP-1,2560x1440@120,0x0,1
# xwayland {
#     force_zero_scaling = true
# }

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

workspace=special:magic,gapsout:240

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

$terminal = ghostty
$editor = nvim
$browser = zen-browser

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf
source = ~/.config/hypr/hyprland-colors.conf

# Some default env vars.
env = HYPRCURSOR_SIZE,36
env = HYPRCURSOR_THEME,rose-pine-hyprcursor
env = XCURSOR_SIZE,36
env = XCURSOR_THEME,BreezeX-RosePine-Linux
env = GTK_THEME,Adwaita:dark

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = cust
    kb_variant = basic
    # kb_model =
    kb_options = ctrl:nocaps
    # kb_rules =
    # kb_file = ~/.config/xkb/cust.xkb

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 20
    border_size = 4
    # Border colors are set dynamically via hyprland-colors.conf (wallust)
    # Fallback values if the sourced file doesn't exist yet:
    # col.active_border = rgba(ecb858ee) rgba(ebd564ee) 45deg
    # col.inactive_border = rgba(595959aa)
    # no_cursor_warps = true

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 7
    
    blur {
        enabled = true
        new_optimizations = true
        size = 6
        passes = 1
        # ignore_opacity = true
        special = true
    }

    shadow {
        range = 4
        render_power = 3
        enabled = yes
        color = rgba(1a1a1aee)

    }
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
	smart_split = yes
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    # new_is_master = true
}

# gestures {
#     # See https://wiki.hyprland.org/Configuring/Variables/ for more
#     workspace_swipe = off
# }

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = 0 # Set to 0 to disable the anime mascot wallpapers
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod SHIFT, L, exec, hyprlock
bind = $mainMod, O, exec, ghostty
bind = $mainMod, Q, killactive, 
bind = $mainMod SHIFT, X, exit, 
bind = $mainMod, B, exec, zen-browser
bind = $mainMod, T, layoutmsg, togglesplit
bind = $mainMod, Z, togglefloating, 
bind = $mainMod, F, fullscreen, 
bind = $mainMod, R, exec, rofi -show drun
bind = $mainMod, V, exec, cliphist list | rofi -dmenu -p "Clipboard" | cliphist decode | wl-copy
bind = $mainMod SHIFT, V, exec, wl-paste --no-newline | DISPLAY=$(tr '\0' '\n' < /proc/$(pgrep -P $(pgrep -x gamescope-wl) | head -1)/environ | grep ^DISPLAY= | cut -d= -f2) xclip -selection clipboard
bind = $mainMod, W, exec, ~/.config/hypr/scripts/wallpaper-picker.sh
bind = $mainMod SHIFT, Z, pin

bind = $mainMod, G, exec, hyprpicker -a
bind = $mainMod SHIFT, G, exec, hyprpicker -a --format=rgb
bind = $mainMod CONTROL, G, exec, hyprpicker -a --format=hsl
bind = $mainMod, A, exec, grimblast copy area
bind = $mainMod SHIFT, A, exec, ~/.config/hypr/scripts/screenshot.sh
bind = $mainMod ALT, A, exec, grimblast --freeze copy area 
bind = $mainMod SHIFT, E, exec, ~/.config/hypr/scripts/record.sh

bind = $mainMod, bracketright, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = $mainMod, bracketleft, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = $mainMod SHIFT, bracketleft, exec, wpctl status | grep -A10 'Sinks:' | grep -m1 'Volt' | tr -d \[:punct:\]\[:alpha:\] | awk '{print $2}' | xargs -I % wpctl set-default %
bind = $mainMod SHIFT, bracketright, exec, wpctl status | grep -A10 'Sinks:' | grep -m1 'HDMI/DP' | tr -d \[:punct:\]\[:alpha:\] | awk '{print $2}' | xargs -I % wpctl set-default %

# Media keys
bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPrev, exec, playerctl previous

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = ALT, down, workspace, -1
bind = ALT, up, workspace, +1
bind = ALT, left, movetoworkspace, -1
bind = ALT, right, movetoworkspace, +1

# Move window across workspace
bind = $mainMod SHIFT, H, swapwindow, l
bind = $mainMod SHIFT, L, swapwindow, r
bind = $mainMod SHIFT, K, swapwindow, u
bind = $mainMod SHIFT, J, swapwindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspacesilent, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, layoutmsg, move -col
bind = $mainMod, mouse_up, layoutmsg, move +col

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# bind = $mainMod, Equal, resizeactive, exact 1280 1080

exec-once=solaar -w hide
exec-once=awww-daemon
# exec-once=hypridle
exec-once=waybar
exec-once=dunst
exec-once=wl-paste --type text --watch cliphist store #Stores only text data
exec-once=wl-paste --type image --watch cliphist store #Stores only image data
# improve volume so it doesn't need high gain
exec-once=sleep 15 && wpctl status | grep -A10 'Sources:' | grep -m1 'Volt' | tr -d \[:punct:\]\[:alpha:\] | awk '{print $2}' | xargs -I % wpctl set-volume % 2
exec-once=hyprctl dispatch workspace 3; raiderio & sleep 8000 | hyprctl dispatch workspace 1
# Pick a random wallpaper on startup and apply full theme
exec-once=sleep 15 && ~/.config/hypr/scripts/random-wallpaper.sh
exec-once=hypridle
