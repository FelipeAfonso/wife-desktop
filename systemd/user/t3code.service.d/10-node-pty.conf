# Drop-in: survives `t3 service install/update` rewriting the main unit file.
# Rebuilds node-pty's linux-x64 native module when a new version ships without it.
[Service]
ExecStartPre=%h/.local/bin/t3-rebuild-node-pty.sh
